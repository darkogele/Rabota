--GO

----MERGE generated by 'sp_generate_merge' stored procedure, Version 0.93
----Originally by Vyas (http://vyaskn.tripod.com): sp_generate_inserts (build 22)
----Adapted for SQL Server 2008/2012 by Daniel Nolan (http://danere.com)

--SET NOCOUNT ON

--MERGE INTO [AspNetUserLogins] AS Target
--USING (VALUES
--) AS Source ([LoginProvider],[ProviderKey],[UserId])
--ON (Target.[LoginProvider] = Source.[LoginProvider] AND Target.[ProviderKey] = Source.[ProviderKey] AND Target.[UserId] = Source.[UserId])
--WHEN NOT MATCHED BY TARGET THEN
-- INSERT([LoginProvider],[ProviderKey],[UserId])
-- VALUES(Source.[LoginProvider],Source.[ProviderKey],Source.[UserId])
--WHEN NOT MATCHED BY SOURCE THEN 
-- DELETE;

--GO
--DECLARE @mergeError int
-- , @mergeCount int
--SELECT @mergeError = @@ERROR, @mergeCount = @@ROWCOUNT
--IF @mergeError != 0
-- BEGIN
-- PRINT 'ERROR OCCURRED IN MERGE FOR [AspNetUserLogins]. Rows affected: ' + CAST(@mergeCount AS VARCHAR(100)); -- SQL should always return zero rows affected
-- END
--ELSE
-- BEGIN
-- PRINT '[AspNetUserLogins] rows affected by MERGE: ' + CAST(@mergeCount AS VARCHAR(100));
-- END
--GO

--SET NOCOUNT OFF
--GO